import CreateProfile from '@/allComponent/CreateProfile/CreateProfile';
import React from 'react';

const page = () => {
    return (
        <div>
            <CreateProfile></CreateProfile>
        </div>
    );
};

export default page;